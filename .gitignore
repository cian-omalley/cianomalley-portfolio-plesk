# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
out/
next-env.d.ts

# production build output (Plesk upload target)
# (kept out of git; regenerate with `pnpm build`)

# misc
*.log
.DS_Store
Thumbs.db

# env
.env
.env.*
!.env.example

# editor
.vscode/
.idea/

# typescript incremental build info
*.tsbuildinfo

# packaged theme (build with ./package.sh)
digital-district.zip

# real-value deploy files (IPs, passwords) — never commit; public repo
local/
